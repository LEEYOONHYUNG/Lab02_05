0. What is your name and student ID?
Hyunjo Jeong / 2020-13680
1. What is .gitignore? You can write the answer either in Korean or English.
불필요한 파일이 github 등의 repo에 올라가지 않도록 하는 것.
2. Why do Github users need an SSH key pair? (Users can use either SSH key pair or github account info.) Please write down a brief explanation of SSH key.
SSH key는 비밀번호 대신 key를 통해 암호화하는 것으로 SSH key pair은 공개키(원격 머신에 저장)와 비공개키(로컬 머신에 저장)로 구성된다. 
접속 시도 시 SSH client가 비공개키와 공개키를 비교해 일치하면 접속을 허가한다. Github는 이 방식을 사용하므로, 유저들은 SSH key가 필요하다.

