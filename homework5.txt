0. What is your name and student ID?
hwangchanghwan, 2020-10347.
1. What is .gitignore? You can write the answer either in Korean or English.
git을 통한 버전 관리에서, git을 통해 관리할 필요가 없는 파일(로그 파일 등)들을 지정할 때 쓰는 확장자이다.
2. Why do Github users need an SSH key pair? (Users can use either SSH key pair or github account info.) Please write down a brief explanation of SSH key.
SSH(secure shell)는 github과 같은 원격 서버에 파일을 복사하는 등의 명령을 실행할 수 있게 해 주는 암호화된 통신 프로토컬이다. SSH key는 이 때의 통신에 사용되는 키를 일컫는다. 이 키가 있는 컴퓨터는 다른 아이디나 암호가 필요 없이 github과 pull, push를 할 수 있다.
