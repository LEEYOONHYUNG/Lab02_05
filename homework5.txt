0. What is your name and student ID?
김동준, 2020-17993
1. What is .gitignore? You can write the answer either in Korean or English.
The .gitignore file is a text file that tells Git which files or folders to ignore in a project.
2. Why do Github users need an SSH key pair? (Users can use either SSH key pair or github account info.) Please write down a brief explanation of SSH key.
By using a SSH keys, user do not have to identify oneself by entering username and password. SSH keys is a pair of keys, one of which is revealed in public, and the other one is private.